/**
 * @param {Program} program 
 */
function init(program) {
    
}

function main() {

}

const { Program } = require("./program");

const program = new Program();

program.on("start", )

program.start();